interface SiteConfig {
  title: string;
  description: string;
}

export const siteConfig: SiteConfig = {
  title: 'MyHome',
  description: 'The place to be for all listings of properties and homes for sale.',
};
